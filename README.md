# How to run 

Go to the root directory after cloning and then:

### Server
Open a new terminal in the root directory in VS code
```
cd server
npm run dev
```

### Client 
Open a new terminal in the root directory in VS code
```
cd client 
npm run dev
```